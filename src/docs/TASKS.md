# Source Tree Tasks

This file tracks only the shared work that still feels worth keeping visible.

## Current Goal

- Keep `src/` easy to navigate while focusing on working code instead of migration scaffolding.

## Active Tasks

- [ ] Keep `src/README.md` and `src/docs/*.md` short and current.
- [ ] Improve active workflows in `src/text/`, `src/cpi/fuel_prices/`, and `src/cpi/price_scraping/` where the pain is real.
- [ ] Add docs only when they remove repeated confusion for a working path.
- [ ] Delete stale planning notes instead of maintaining them.

## Not Doing

- [ ] No new migration structure unless code is actually moving.
- [ ] No repo-wide metadata or contract rollout without a concrete operational need.
