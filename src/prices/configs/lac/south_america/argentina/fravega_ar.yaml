scaffolding: spider
extraction_pattern: scrapy_api
analytical_role: retailer_sku
coicop_classification: classifier
spider: fravega_ar
url: https://www.fravega.com/
language: es
currency: ARS
cadence: weekly
channel: electronics
archive_prefix: "www.fravega.com/p/"
archive_path_re: '^/p/[^/]+/$'
notes: |
  Electronics/appliances chain, independent VTEX tenant. Live re-verified 2026-08-17:
  products/search?_from=0&_to=4 -> HTTP 206, real JSON (e.g. 'Aire Acondicionado Split
  Frio/Calor Hisense 3400W'). Currency ARS, matches countries.yaml. Full scrape: <ROWS> rows.
  Common Crawl archive note: the live scraper's current product URLs end in
  `<slug>-<id>/p`, but that shape is 0-for-0 in CC-MAIN-2026-30 -- the crawl only
  holds an older URL scheme, `/p/<slug>-<id>/` (8,965 records within the first 4
  cdx blocks, 100% match against that shape). archive_path_re targets the CC-era
  shape, not the current live-scrape shape.
timeout: 28800
