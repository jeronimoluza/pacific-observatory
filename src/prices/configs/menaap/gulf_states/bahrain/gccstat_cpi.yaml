scaffolding: fetcher
extraction_pattern: rest_api
analytical_role: cpi_benchmark
coicop_classification: publisher_labeled
coicop_codes: ["01", "02", "03", "04", "05", "06", "07", "08", "09", "10", "11", "12"]
channel: null
source_key: bh_gccstat_cpi
module: menaap.gulf_states.bahrain.gccstat_cpi
function: fetch_bh_gccstat_cpi
url: https://dp.marsa.gccstat.org/dataset/consumer-prices
language: en
cadence: monthly
fallback_date: 2014-12-01
notes: |
  Re-verified live 2026-08-06: GCC Statistical Centre (GCC-Stat) harmonised
  regional CPI, dataset GCCSTAT.ES,DF_ES_CPI,1.0 ("Consumer Prices"), served
  via a Fusion Registry SDMX REST endpoint behind the DKAN data portal at
  dp.marsa.gccstat.org. Bucket-2 shared module
  (src/prices/fetchers/_shared/menaap/gccstat_cpi.py) — one CSV covers all
  six GCC states plus a regional aggregate row; this manifest is the Bahrain
  wrapper. 36,765 total rows across all countries, monthly + annual +
  pct-change series; only UNIT="Index"/FREQUENCY="Monthly" rows kept here.
  Fresh through 2025-12 at probe time (~8-month lag, typical for a
  harmonised regional CPI). 12-indicator COICOP-2018-style grouping
  (division 13 absent, same pattern as Bahrain's own national CPI portal);
  all-items headline indicator dropped, no sanctioned sentinel yet.
  Cross-check against bh_cpi_odp.yaml (Bahrain's own national CPI, also
  onboarded this round) — two independent publishers, useful as a
  consistency check on the same country.
  coicop_divisions: 01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12
  divisions_basis: walked_categories (all 13 INDICATOR values enumerated from the full CSV)
