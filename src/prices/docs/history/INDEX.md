# Prices — History (frozen archive)

Durable findings and decisions curated from the old `.planning/` tree. Docs here
are **frozen** — once archived they are not edited. They may describe past state;
trust the living docs (`../ARCHITECTURE.md`, `../ENRICH.md`, `../GLOSSARY.md`) for
what is true today.

## Status: triage pending

The `.planning/` tree (~180 markdown files across `phases/`, `experiments/`,
`notes/`, `spikes/`, `seeds/`, `research/`, `tier-a-fix/`) has not yet been
triaged into this archive. That was deliberately deferred (living docs first).

When triage runs, each `.planning` doc is classified **KEEP** (a durable finding
still worth reading), **SUPERSEDED** (interesting but overtaken — linked, not
copied), or **DROP** (scratch/logs/venv noise). KEEP docs are copied here with a
one-line deprecation header and indexed below, newest-first. Until then, the raw
archive lives untouched at `.planning/`.

## Archived docs

_(none yet — triage pending)_
