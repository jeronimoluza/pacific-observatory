"""Tier (b) — cluster-resolved KNN over the enrichments cache.

Package home for the tier-b layer, mirroring stages/ / keywords/ /
regex_patterns/. Import sites use the submodule paths directly, e.g.
``from prices.enrich.tier_b import index as tier_b_index``.
"""
