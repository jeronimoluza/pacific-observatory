# Unusable with current pipeline strategies (deferred, not fabricated).
# Reason: no WordPress API (wp-json 404, custom CMS). Listing pages
#   (/latest, /morearticles/<section>) return identical article sets
#   regardless of ?page=N / /latest/2 — pagination is client-side/AJAX and
#   the real "load more" endpoint could not be identified via static
#   httpx/BeautifulSoup probing. Would need JS-driven or API reverse-
#   engineering (Tier 2).
name: Oman Observer
language: en
country: oman
base_url: https://www.omanobserver.om
