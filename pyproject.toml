[project]
name = "pacific-observatory"
version = "0.1.0"
description = "Compiled analytics supporting the World Bank's Pacific Observatory"
authors = [
    {name = "Pacific Observatory"}
]
readme = "README.md"
requires-python = ">=3.11,<4.0"
dependencies = [
    "pytest (>=8.4.2,<9.0.0)",
    "scikit-learn (>=1.7.2,<2.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
scraping = [
    "bs4 (>=0.0.2,<0.0.3)",
    "lxml (>=6.0.1,<7.0.0)",
    "numpy (>=1.18.5,<2.0)",
    "pandas (>=2.3.2,<3.0.0)",
    "pip (>=25.2,<26.0)",
    "pycookiecheat (>=0.8.0,<0.9.0)",
    "python-dateutil (>=2.9.0.post0,<3.0.0)",
    "requests (>=2.32.5,<3.0.0)",
    "selenium (>=4.35.0,<5.0.0)",
    "tqdm (>=4.67.1,<5.0.0)",
    "urllib3 (>=2.5.0,<3.0.0)",
    "asyncio (>=4.0.0,<5.0.0)",
    "pyyaml (>=6.0.2,<7.0.0)",
    "httpx (>=0.27)",
    "pydantic (>=2.11.9,<3.0.0)",
    "spacy (>=3.8.7,<4.0.0); python_version >= '3.11' and python_version < '3.13'",
    "gensim (>=4.3.3,<5.0.0)",
    "matplotlib (>=3.10.6,<4.0.0)",
    "nltk (>=3.9.2,<4.0.0)",
    "sdmx1 (>=2.23.1,<3.0.0)",
    "scrapy (>=2.13.4,<3.0.0)",
    "thefuzz (>=0.22.1,<0.23.0)",
    "openpyxl (>=3.1.5,<4.0.0)",
    "google-generativeai (>=0.8.5,<0.9.0)",
    "waybackpy (>=3.0.6,<4.0.0)",
    "scrapy-playwright (>=0.0.45,<0.0.46)",
    "pyahocorasick (>=2.3.0,<3.0.0)",
    "googletrans (>=4.0.2,<5.0.0)",
    "pdfplumber (>=0.11.9,<0.12.0)",
    "xlrd (>=2.0.2,<3.0.0)",
    "pyxlsb (>=1.0.0,<2.0.0)",
    "tesseract (>=0.1.3,<0.2.0)",
]
docs = [
    "docutils (==0.17.1)",
    "jupyter-book (==0.15.1)",
    "jupyter-cache (==0.5.0)"
]
testing = [
    "anyio (>=4.11.0,<5.0.0)",
    "pytest-asyncio (>=1.2.0,<2.0.0)",
    "pytest-tornasync (>=0.6.0.post2,<0.7.0)",
    "pytest-trio (>=0.8.0,<0.9.0)",
    "pytest-twisted (>=1.14.3,<2.0.0)",
    "twisted (>=25.5.0,<26.0.0)",
    "pytest-httpx (>=0.30.0,<0.31.0)",
    "pytest-cov (>=4.1.0,<5.0.0)",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "unit: Unit tests (no network, no external dependencies)",
    "integration: Integration tests (may require network)",
    "slow: Tests that take more than 10 seconds",
]
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
source = ["src/text"]
omit = [
    "*/tests/*",
    "*/__init__.py",
    "*/plotting/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = [
    "tests/",
    "docs/",
]

[tool.black]
line-length = 100
target-version = ["py311"]
exclude = '''
/(
    \.git
    | \.venv
    | build
    | dist
    | docs
)/
'''

[tool.isort]
profile = "black"
line_length = 100
skip = [".git", ".venv", "build", "dist", "docs"]
