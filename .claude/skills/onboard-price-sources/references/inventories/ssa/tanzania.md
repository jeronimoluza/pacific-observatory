# Tanzania

_Inventory written: 2026-09-01_

Wave 12 pass. Already-covered before this pass: `fastandfresh_tz` (supermarket),
`wfp_prices` (official_avg) — 2 sources / 1 food. This pass needed 3 more sources
AND 1 more food; target was >=5 sources AND >=2 food. Ended at **5 sources / 2 food**.

| Source name | URL | Channel | Status | Notes |
|---|---|---|---|---|
| EWURA fuel caps | https://www.ewura.go.tz/publications/petroleum-price | fuel-station | **SHIPPED** as `ewura_fuel_caps` | Monthly "Cap Prices for Petroleum Products" PDF, Table 3 lists Petrol/Diesel/Kerosene per town (~186-190 towns/month) — a genuine regional monthly time series with effective dates, exactly the brief's recommended build. `PriceObservation`, `period_kind: effective_from`, `subnational_area` in `_IDENT`. Verified live 2026-09-01: 5,679 rows, 0 duplicate `observation_hash`, 431 distinct subnational_area strings (true town count ~190/month; higher string count is a cross-month PDF-text-extraction spacing artifact — e.g. "Pangani" renders as "P angani" in the August 2026 PDF — not a duplication defect). 100% TZS, price range 2,653-4,950/L. THESE ARE CAPS, NOT TRANSACTION PRICES — stated explicitly in the YAML notes per the brief's instruction. June 2026's listed PDF (`en-1780433786`) 404s live — EWURA's own dead link, that month is silently absent from the series. |
| NBS Tanzania CPI | https://www.nbs.go.tz/statistics/topic/consumer-price-index-2026 | null (cpi_benchmark) | **SHIPPED** as `nbs_cpi` | Monthly "CPI Summary_MMYYYY.xls" download; each release conveniently republishes the FULL 2020-12-present series across one sheet per year, so one download reconstructs the whole history. 13-row major-groups block maps 1:1 to COICOP-2018 divisions in S/N order. Verified live 2026-09-01: 884 rows (68 months x 13 divisions), 0 duplicate `observation_hash`, index range 98.9-138.1 (base 2020=100). GOTCHA: listing URL is year-suffixed (`consumer-price-index-2026`) and needs a manual bump every January — the un-suffixed page 200s but carries no file links. |
| NidaDanish | https://www.nidadanish.com/ | hypermarket | **SHIPPED** as `nidadanish_tz` | Found via Swahili/English discovery search (not the two workbook candidates, both dead — see below), surfaced when checking `nidadanish.com/grocery/`, itself linked from a search result about Fast & Fresh (the two sites' meta descriptions are cross-contaminated, coincidence of a shared page-builder template, NOT the same company/backend — verified no shared product_id namespace or footer branding, rule 19 checked). CS-Cart storefront, ~617 categories from large appliances through groceries (rice-sugar, cooking-oil, tea-coffee, snacks, dairy, etc. all carry real TZS-priced Tanzanian SKUs). Runs CS-Cart *Multi-Vendor* edition but the buyer UI fully hides vendor identity (`vendor.show_name_as_link`/`show_logo` both "N" in the embedded theme config, `/vendors/` and `/sellers/` both 404) — no reachable seller directory, so rule 14's "split into named merchants" doesn't apply; classified `channel: hypermarket`, not `marketplace`, because the storefront presents as one general retailer to a shopper (flag for re-review if a public vendor directory later appears). Verified live 2026-09-01 with a 5-minute time-capped crawl (all 617 categories seeded as start URLs up front, so the sample isn't LIFO-tail-biased per rule 3): 12,622 rows, 12,622 distinct product_id/url, 0 zero-price, 0 blank names, 100% TZS, median 39,000 (range 1,000-100,000,000 — the top of the range is genuine industrial diesel generators). Food-and-beverage categories were 6.0% of this partial-crawl sample (757/12,622) — reported as an estimate, not the whole-catalog figure, since only a 5-minute slice of a 617-category site was crawled. 2-3 emitted product URLs re-fetched cold, all matched (name + price unchanged). |
| Jambo Supermarket (workbook candidate) | https://www.jambosupermarket.online/ | — | **DEAD — demo/seed store** | React SPA backed by a public-anon Supabase table (`products`, no auth needed — the anon key is client-embedded by design). Only 34 rows, ALL with `created_at` identical to the microsecond (`2026-06-07T20:02:14.630559+00:00`), Unsplash stock photos, generic template names ("Whole Wheat Bread", "Farm Fresh Eggs (30 pcs)"). Textbook wave-10-rule-20 seed/demo pattern (same shape as the "test phone" stores from wave 10) — not a real catalogue. |
| duka.direct (Selcom) (workbook candidate) | https://duka.direct/ | — | **DEAD — app-only, no web catalogue** | Homepage is a Tilda page-builder marketing site (not the actual app), linking out only to `get.duka.direct` (an app-install redirector that itself timed out). No product/store data reachable from the web domain at all — matches the brief's own caution that this is Selcom's app, and the web presence turns out to be pure marketing. |
| Imadi MbogaMboga | https://imadi.co.tz/ | — | **DEAD — app-only, no web catalogue** | Found via English search ("supermarket online Dar es Salaam"). Static one-page marketing site for a produce-delivery app; every CTA points to app-store download links, no web ordering/catalogue. |
| Piki | https://www.piki.co.tz/ | — | **NOT PURSUED — marketplace, business-listing API not cracked in budget** | Found via English search. Real SPA on the `ordering.co` / `orderingplus.com` whitelabel food-delivery platform (`api.orderingplus.com/v400/en/onlinefooddelivery/...`), confirmed via Playwright network capture. The public `business` list endpoint returned 0 results / 401 on the request shapes tried (JSON `params=`, bracket `params[location][lat]=`) without a location first being set through the UI flow; did not reverse-engineer further given the food-and-beverage bar was already clear via NidaDanish. Worth a real look in a future pass: Piki markets itself as covering "groceries, drinks and mobile phones" alongside restaurants, so a working business-list call might yield real named grocery merchants splittable under rule 14. |
| Nidadanish sibling leads (Tiketi, Instagrocer) | https://tiketi.com/online-grocery-delivery-dar-es-salaam-store/, https://www.instagrocer.co/delivery/dar-es-salaam | — | **NOT PROBED — found in search, not investigated** | Surfaced by the same English search as Piki/Imadi. Not fetched or fingerprinted this pass since NidaDanish had already cleared the food bar; flagging so a future pass doesn't have to re-run the same search from zero. |
| TANESCO electricity tariff | https://www.tanesco.co.tz/ | — | **DEAD (for now) — image-only PDF** | Angular SPA; real backend at `www.tanesco.co.tz/api/v1/...`. The `documents` endpoint (public, no auth) has a `TARIFFS`-category document, "TANESCO approved Tariffs" → `https://www.tanesco.co.tz/api/v1/uploads/tanesco_approved_tariffs.pdf` — but that PDF is a single-page scanned image (0 extractable chars, 5 embedded images, `extract_tables()` empty). OCR (pytesseract) could recover it but digit-level OCR errors on a price/tariff table are a real risk for financial data; not attempted given the source bar was already met without it. Worth a real OCR attempt in a future pass if TANESCO becomes a priority (rule 16 non-retail sources are usually cheap, but this one specifically is not, because of the scan).

## COICOP / channel gap after this pass

Tanzania ends at 5 sources / 2 food (existing `fastandfresh_tz` supermarket + new
`nidadanish_tz` hypermarket). Non-food additions this pass (`ewura_fuel_caps` tariff,
`nbs_cpi` cpi_benchmark) cover COICOP 07.2.2/04.5.4 (fuel) and all 13 divisions at the
index level (CPI) respectively, but do not add price-level food coverage beyond
NidaDanish's own catalog. A genuine additional food-coverage lift would come from either
(a) cracking Piki's business-listing API to split out named grocery merchants under rule
14, or (b) a fresh discovery pass specifically for Arusha-based grocers (this pass's
searches were Dar es Salaam-heavy; Arusha was named in the brief but not separately
searched). TANESCO tariff (electricity, non-food, `tariff`/`fuel-station`-adjacent
channel) remains a cheap-in-principle but OCR-blocked non-food source for a future pass.
