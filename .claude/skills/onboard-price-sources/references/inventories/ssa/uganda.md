# Uganda

_Inventory written: 2026-09-01_

Wave 9 pass (cold-start; no prior inventory file existed for Uganda). Already-covered
before this pass: `elyahwehventures_ug` (supermarket, the one food source), `wfp_prices`
(official_avg, shared SSA fetcher) — 2 sources / 1 food. Target was >=5 sources AND >=2
food. This pass needed 3 more sources, at least 1 food.

**Result: 5 sources / 2 food. Target met.**

| Source name | URL | Channel / role | Status | Notes |
|---|---|---|---|---|
| Supermarket.co.ug | https://supermarket.co.ug/ | `supermarket` | **SHIPPED** as `supermarket_co_ug` | PrestaShop storefront (itemprop-tagged category HTML), scaffolded on `_prestashop_base`. 172 unique products verified live 2026-09-01 across mostly-food categories (vegetables, fruit, dairy, meat/poultry, cheese, frozen, pasta, sodas, oils, ~94% food share by URL category segment) plus a small non-food tail (electronics, fragrance, flowers, wine). **Currency/locality flag**: prices are rendered in USD (not UGX) despite delivering within Kampala — reads as a diaspora "order groceries for delivery to family in Uganda" model rather than an ordinary domestic shelf price; schema.org `priceCurrency` even says "RON" (a stale PrestaShop template default, not a real price). Kept per onboarding rule 8 (real Uganda-delivering retailer, currency flagged loudly in the YAML `notes:`) but downstream should not treat it as a domestic UGX series. |
| UBOS Consumer Price Index | https://www.ubos.org/consumer-price-index/ | `cpi_benchmark` | **SHIPPED** as `ubos_cpi` (fetcher) | Discovered live via UBOS's WordPress REST API (`/wp-json/wp/v2/posts?search=Consumer+Price+Index`), not hardcoded. Latest post "Consumer Price Index August 2026" links a "CPI Excel Tables" xlsx with a "Division " sheet whose 13 rows are the COICOP-2018 13 divisions **in the standard order**, 1:1 — no translation map needed, just `publisher_labeled`. Backfilled 1,027 IndexObservation rows (Feb 2020 - Aug 2026, 13 divisions x 79 months) on first run; idempotent on re-run (0 new rows at cutoff=2026-08-01). Needs `verify=False` — www.ubos.org serves its leaf cert without the intermediate (fails Python's certifi chain, fine in curl/macOS), same class of quirk as `vnso_cpi.py`/`statsdiv_cpi.py` elsewhere in this repo. |
| ERA End-User Electricity Tariff | https://www.era.go.ug/tariff-schedules/ | `tariff` (COICOP 04.5.1) | **SHIPPED** as `era_electricity_tariff` (fetcher) | ERA's "tariff-schedules" landing page itself carries no table or download — the real content is a quarterly WordPress press-release post (found the same way as UBOS, via `/wp-json/wp/v2/posts?search=...`) that states all 9 consumer-category tariffs plus Lifeline and Cooking bundle rates as one plain-text sentence. Regex-parsed 11 PriceObservation rows from the July 2026 ("Third Quarter of 2026") post; idempotent on re-run. No TLS workaround needed for era.go.ug (unlike ubos.org). |
| Carrefour Uganda | https://www.carrefouruganda.com/ | would be `hypermarket` | **DEAD — Akamai, same MAF tenant as the Gulf** | HTTP 403, 377-byte "Access Denied" `errors.edgesuite` stub, fails `curl_cffi` on all 3 impersonation profiles (chrome124/chrome120/safari17_0). Identical signature/reference-id format to the already-documented `carrefourqatar.com`/`carrefourksa.com`/`carrefouruae.com` Akamai tenant in `known_blockers.md` — confirms the MAF/Algolia stack extends to this East Africa storefront. Real local footprint exists (Acacia/Lugogo/Metroplex/Oasis Mall, Kampala, per the brand's own marketing copy) but no network trace beyond the front page was attempted (time/WebSearch budget) — worth a Playwright-discover pass if this tenant is ever prioritized as a dedicated multi-hour effort. |
| Carrefour Uganda via Glovo | https://glovoapp.com/ug/en/kampala/carrefour-kpa/ | would be `supermarket` (delivery-app pattern) | **PROBED, NOT SHIPPED — real lead for next pass** | Storefront page itself loads fine (200, curl_cffi chrome124) but is a client-hydrated SPA with no embedded product JSON. Playwright network trace found the real API host (`api.glovoapp.com`) and resolved the store id (256958) + address id (613064) from captured XHRs, but the product-listing endpoint (`/v3/stores/{id}/addresses/{addr}`) requires a `Glovo-Perseus-Session-Id` header bootstrapped through a guest-device registration flow (`identity/v4/devices`, which 429'd in this sandbox) — session-bootstrap plumbing, not a simple no-auth JSON endpoint. Not built this pass: the 5/2 bar was already cleared by `supermarket_co_ug` + the two fetchers, and this needs real scaffolding effort (a session-warmup step before every crawl) that deserves a dedicated pass rather than being squeezed in. Capital Shoppers is also on Glovo (`+256 41 4254523`, per a web search) and would likely hit the same session-bootstrap requirement — worth doing both together once the Glovo session flow is solved. |
| Jumia Uganda | https://www.jumia.ug/ | marketplace | **DEAD — Cloudflare challenge, confirmed shared tenant** | HTTP 403, `<title>Just a moment...</title>`, fails `curl_cffi` on all 3 impersonation profiles. Same signature already confirmed dead today on `jumia.com.gh` and `jumia.com.ng` (both wave-9 probes, same session) — treated as the same Cloudflare tenant rather than re-running the mandatory Playwright network-trace gate a third time in one day. No reachable seller-directory alternative (same domain, same block). |
| FEWS NET (`fdw.fews.net`) | https://fdw.fews.net/api/marketpricefacts/ | would be `official_avg` | **DEAD (right now) — but a REGRESSION, not Uganda-specific** | Uganda was never wired into the existing shared `_shared/ssa/fews_net.py` fetcher (only 13 SSA countries are, none East African except Rwanda). Attempted to add it, found the *entire* API now sits behind an AWS WAF `awswaf.com` challenge page (HTTP 202 + `window.gokuProps` JS-challenge stub) on every `country_code` tested, including `RW` — one of the 13 countries the module verified working on 2026-08-07. This is a platform-wide regression since that verification, not a per-country gap. Recorded in `known_blockers.md` under AWS WAF; do not add Uganda (or re-verify any other FEWS NET country) until the tenant-wide block is re-probed and clears. |
| Quality Supermarket, Capital Shoppers, Mega Standard, Ebenezer | — (no independent domain found) | — | **NO ONLINE CATALOGUE FOUND** | All four are real, well-known locally-owned Kampala chains (Quality Supermarkets since 1980, Capital Shoppers since 1997, both "largest locally owned" per their own Wikipedia entries) but neither has an independently reachable e-commerce domain — `qualitysupermarket.co.ug`, `qualitysupermarketug.com`, `capitalshoppers.co.ug`, `capitalshoppersug.com`, `megastandard.co.ug`, `ebenezersupermarket.co.ug`, `ebenezer.co.ug` all fail DNS resolution. Capital Shoppers' own online-shopping/home-delivery claim (per a web search) resolves to Facebook + Glovo, not a standalone site. Mega Standard and Ebenezer were not further searched (WebSearch budget) beyond the initial DNS-guess sweep. If revisited, search for their actual domains by name rather than guessing `.co.ug` variants. |
| UBOS "average retail prices" table | https://www.ubos.org/ | would be `official_avg` | **NO QUALIFYING PUBLIC SOURCE FOUND** | The brief expected a UBOS `official_avg` companion to the CPI (per-item average prices in currency, mirroring what several other SSA NSOs publish). Checked via the same WordPress REST search used for the CPI (`search=average retail prices` / `retail prices` / `food prices`) — UBOS publishes CPI (monthly) and PPI-Manufacturing&Utilities (monthly) news posts, but no recurring item-level average-retail-price release was found. Recorded as a confirmed dead end, not an oversight — do not re-search this without a new lead. |
| grocerynowstore.com, farmiketug.com, gategrocer.com, eshopuganda.com | — | — | **NOT REACHED / inconclusive, worth a re-check** | Surfaced by one web search for "Uganda online grocery shop" alongside `supermarket.co.ug` (which was pursued and shipped). `grocerynowstore.com` timed out (20-30s) on `curl_cffi`; `farmiketug.com` failed DNS (brand name may not match the actual domain — check for a `farmiket.com` or similar without the "ug" suffix); `eshopuganda.com` returned HTTP 409 on the front page (unclear cause, not investigated further); `gategrocer.com` was not probed at all (WhatsApp-order model per its own search snippet, likely no web catalogue). None were pursued once the 5/2 bar was already cleared by `supermarket_co_ug` — worth a fresh probe pass if Uganda's food count needs to grow further.

## COICOP / channel coverage after this pass

Uganda: 5 sources / 2 food.
- **01 Food and non-alcoholic beverages**: `elyahwehventures_ug` (supermarket, retailer_sku), `supermarket_co_ug` (supermarket, retailer_sku, USD-priced — see flag above), `wfp_prices` (official_avg, food commodities only).
- **cpi_benchmark (all 13 COICOP divisions, index-level)**: `ubos_cpi`, monthly, back to Jul2017=100 base.
- **04.5.1 Electricity**: `era_electricity_tariff`, quarterly, UEDCL end-user tariff by consumer category.
- Everything outside food/electricity/the CPI benchmark (clothing, furnishings, health, transport ex-electricity, communication, recreation, education, restaurants, insurance, personal care/misc — COICOP 02-13 at the retailer_sku/official_avg/tariff level) remains **uncovered** for Uganda after this pass. The clearest next-pass leads are: (1) the Glovo session-bootstrap (unlocks Carrefour + Capital Shoppers as food/13-mixed retailer_sku sources), (2) NWSC water tariff and MTN/Airtel Uganda telecom plan pages (named in the wave-9 brief, not reached this pass — budget went to the fetchers that were reached first), (3) the four unprobed grocery domains above.
