# Ghana

_Inventory written: 2026-09-01_

Wave 7 pass. Already-covered before this pass: `ghbasket_gh` (supermarket), `konzoom`
(supermarket), `tezeon_gh` (marketplace) — 3 sources / 2 food. This pass needed 2 more
sources of any channel; target was >=5 sources AND >=2 food.

| Source name | URL | Channel | Status | Notes |
|---|---|---|---|---|
| CompuGhana | https://compughana.com/ | electronics | **SHIPPED** as `compughana_gh` | Shopify, open `/products.json`, no auth. 2,086 items verified live 2026-09-01. Currency GHS confirmed via on-page GH₵/GHS markup. |
| Franko Trading Enterprise | https://frankotrading.com/ | electronics | **SHIPPED** as `franko_trading_gh` | Public site is a Vite SPA; real backend is `https://testing.frankotrading.com/GetCTP001Products` with a client-embedded `x-api-key` (found in the JS bundle, no further auth). 8,637 items verified live 2026-09-01, clean pagination termination (page 5/5 short). No category or server-rendered PDP in the payload — `url` is a constructed `/product/<id>` route. |
| Melcom (melcom.com) | https://melcom.com/ | would be `hypermarket` or `dept-store` (carries food/beverages + household + electronics) | **PROBED, NOT SHIPPED — real lead for next pass** | Ghana's largest retail chain. Front door is Sucuri Cloudproxy: a JS-eval challenge that sets a `sucuri_cloudproxy_uuid_*` cookie via `document.cookie` + `location.reload()` — bare `curl_cffi` (chrome124) gets stuck in the redirect loop, but headless Playwright clears it in ~5s (real Magento storefront, theme `bs_ecolife`, confirmed via `/static/version.../Magento_Catalog/...` JS paths). **Working bypass found and verified**: solve the challenge once with Playwright, extract the `sucuri_cloudproxy_uuid_*`+session cookies, then replay them with `curl_cffi impersonate="chrome124"` — a plain PDP fetch (`/5-star-kaesar-cola-drink-350ml.html`) returned HTTP 200 with `og:price:amount=2.99`/`og:price:currency=GHS` and a visible `<span class="price">₵2.99</span>`. `/graphql` and `/rest/V1/products` are behind the *same* Sucuri gate (not a separate open API), so the whole-catalog walk has to go through SEO-friicf.js pdp URLs, not a JSON endpoint. Homepage link-harvest surfaced ~288 direct product/category URLs incl. real food/beverage SKUs (soft drinks, alcohol) alongside appliances and household goods — genuinely wide catalog, `channel` would need to be decided once volume across divisions is seen (leaning `hypermarket`). **Not shipped this pass** because reusing a Playwright-derived cookie inside Scrapy's own (non-curl_cffi) downloader is unverified — Scrapy's TLS stack may not match the fingerprint the cookie was validated against, and building+testing a cookie-bootstrap middleware was out of scope for a 2-source ask that was already met by the two electronics wins above. **Recommended next step**: a CrawlSpider seeded from the harvested product/category URLs, with a `SpiderMiddleware`/`__init__`-time subprocess call to a small sync-Playwright script that fetches the challenge cookie once and injects it into every `scrapy.Request`'s `Cookie` header (verify with `curl_cffi` still, not plain `requests`/Twisted, in case Sucuri also checks TLS — if Twisted alone 403s despite the cookie, the middleware needs to route through `curl_cffi` per-request instead of Scrapy's default downloader). |
| Jumia Ghana | https://www.jumia.com.gh/ | marketplace | **DEAD — Cloudflare challenge** | HTTP 403 `<title>Just a moment...</title>` on curl_cffi (chrome124/chrome120/safari17_0) AND headless Playwright. See `known_blockers.md` § Cloudflare interactive challenge. No `jumia_*` spider exists anywhere in this repo to reuse a template from. |
| Shoprite Ghana | https://www.shoprite.com.gh/ | — | **DEAD — no online store** | Resolves to Shoprite Holdings' generic pan-African AEM corporate portal (`shopriteafrica` clientlibs, page title just "Home"), not a Ghana storefront. Consistent with the brief's note that Shoprite exited some West African markets. |
| China Mall Ghana, Palace Mall/Supermarket, MaxMart, Game Ghana, Telefonika, mPharma/QualityRx, Ernest Chemists, Koala Shopping Centre, Bolt Food/Glovo Ghana grocery verticals | — | — | **NOT PROBED** | Named in the wave-7 brief but not reached this pass — the two electronics wins (CompuGhana, Franko Trading) already cleared the "any channel" bar, so remaining budget went to Melcom (a bigger, food-bearing lead) instead of working further down this list. Open for a future pass; MaxMart and Palace Supermarket are the more food-relevant of the group. |

## COICOP / channel gap after this pass

Ghana sits at 5 sources / 2 food (both existing supermarkets: `ghbasket_gh`, `konzoom`).
The two new sources (CompuGhana, Franko Trading) are both `electronics` — pure
non-food division-05/08/13 SKUs, no COICOP-01 lift. Melcom is the one lead on this list
that would genuinely widen food/beverage coverage if built.
