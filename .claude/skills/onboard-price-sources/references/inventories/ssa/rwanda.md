# Rwanda

_Inventory written: 2026-09-01_

Wave 12 pass (cold-start; no prior inventory file existed for Rwanda). Already-covered
before this pass: `fews_net` (official_avg, shared SSA fetcher), `wfp_prices`
(official_avg, shared SSA fetcher) — 2 sources / 0 food, both humanitarian market
surveys, zero retail coverage. Target was >=5 sources AND >=2 food. This pass needed
3 more sources, at least 2 food. All four candidates handed off by the brief were rated
workbook `ACCEPT` (`sources_pending_will.xlsx`, "Pending sources" sheet, rows tagged
`P1 - country has ZERO sources`); `sources_pending_jero.xlsx` had zero Rwanda rows.

**Result: 5 sources / 2 food. Target met exactly (not padded above it).**

| Source name | URL | Channel / role | Status | Notes |
|---|---|---|---|---|
| N.Honest Supermarket | https://www.honestsupermarket.com/ | `supermarket` | **SHIPPED** as `nhonest_rw` | Workbook flagged "product API returned HTTP 403 on crawl" — that was a bare-curl false verdict; `curl_cffi impersonate=chrome124` gets a clean 200 and the same-origin JSON API (`/api/products`, `/api/categories`, `/api/settings/public`) is wide open, no auth, no anti-bot at all. Fully server-side paginated (53 pages x 100), plain HTTP, no Playwright. Verified live 2026-09-01: 5,196 valid rows (5,206 total products, 10 dropped by price/name validation) across 29 categories — genuine general-merchandise supermarket (groceries, fresh food/fish/meat, beverages, dairy, personal care, household, clothing, stationery). Food share (fresh food + packaged food + drinks categories) ~32%. Zero duplicate product_id/url, zero blank names, zero non-positive prices, currency 100% RWF, median price 7,000 RWF (range 100-245,000). Site has no per-product detail route (single-page catalogue) — DuplicationPipeline dedups on `item['url']`, so rows carry a synthetic `#product-<_id>` fragment on the site root per onboarding rule 9. 3 cold re-fetched product_ids matched shipped name+price exactly. |
| haha.rw | https://haha.rw/ | `fresh-market` | **SHIPPED** as `haha_rw` | React/Vite SPA — homepage ships an empty `#root` div (779-byte curl response), looks dead on a bare fetch. The JS bundle (`/assets/index-*.js`) references a wide-open, unauthenticated JSON API at `api.haha.rw` (`/items`, `/categories`, `/local-markets`) — "Playwright to discover, plain HTTP to scrape". Verified live 2026-09-01: 58 items total (single page, no pagination needed), 4 categories (Vegetables and Legumes 35, Fruits 15, Meat 6, Grain 2), exactly 1 active market ("Nyabugogo Market", Kigali — a named physical transport/wholesale/food hub, not a virtual aggregate) → genuinely fresh produce/meat/grain, `channel: fresh-market`, counts as food. 100% food, zero dupes, zero blank/zero-price rows, currency 100% RWF, median 1,200 RWF. React router exposes a client-side `/product/:productName/:id` route, used to build a real (if client-rendered) per-product URL. 3 cold re-fetched items matched exactly. Small catalogue (58 SKUs) — a boutique single-market app, not a full grocery chain; do not expect it to carry non-food depth. |
| MTN Rwanda prepaid bundles | https://www.mtn.co.rw/bundle-category/data-bundles/ (+ `/bundle-category/voice/`) | `tariff`, COICOP 08.1.0 | **SHIPPED** as `mtn_rw_bundles` (fetcher) | WordPress + Elementor site, fully server-rendered — no JS/anti-bot. Each bundle is an Elementor "loop-item" whose CSS class carries validity (`bundle-type-daily/weekly/monthly`), with a DATA heading (+ optional MIN heading for voice+data combos) and a price button (e.g. "Rwf 7000"). Verified live 2026-09-01: 10 rows from data-bundles (5 weekly/4 monthly/1 daily, 1MB-30720MB) + 7 rows from voice (4 monthly combo/3 daily combo, 5MB-40GB data + 20min-7500min voice) = 17 rows, zero duplicate `observation_hash`. RWF is integer — prices are always whole francs on this site, no fabricated decimals. Cold re-fetch of 3 bundle rows matched exactly. This is the rule-16 "cheapest build" non-food source that closed the 5th-source gap; picked over NISR/RURA/REG because it needed zero PDF/table parsing. |
| Fasita | https://fasita.rw/ | would be `supermarket`/`marketplace` | **PROBED, DEAD (near-empty catalog)** | Real Next.js frontend + genuinely open JSON API (`/api/products`, `/api/categories`, sniffed live via Playwright network capture, no auth needed). But `GET /api/products` returns exactly **1** product ("Jibu Water", 3472.00 RWF) across every param combination tried (plain, `category_id=`, `all=1`, `limit=200`), and 11 of 12 categories show `"products":0`. The workbook's "Live: Kigali, Huye, Musanze, Nyanza; groceries and essentials" claim does not match a live catalog this size. Fails the >=5-rows gate by a wide margin. Recorded in `known_blockers.md` under "Placeholder / seed demo-data catalog". Do not build without re-checking in ~6 months. |
| Pride Farms | https://www.pridefarms.rw/ | would be `fresh-market` | **PROBED, DEAD (near-empty catalog)** | Wix Stores storefront. The GraphQL storefront API (`/_api/wix-ecommerce-storefront-web/api`) needs a signed visitor `instance` token — obtained via `GET /_api/v1/access-tokens`, keyed by the Wix Stores appDefId `215238eb-22a5-4c36-9e7b-e7c08025e04e` (not the first app entry in the token payload, which is a different, unrelated app) — but even once auth is sorted, the resolved catalog JSON is only available inline in Playwright's post-hydration DOM (`productsWithMetaData`), not via a separate XHR the GraphQL query alone can drive from a plain HTTP session. More importantly: the catalog itself is nearly empty. `/shopall` ("All Products") reports `totalCount: 1`; `/fruits`, `/vegetables`, `/dairy`, `/poultry` all report `totalCount: 0`; only `/pulses` carries the same single SKU ("Chickpeas Kabuli whole G1-1kg", RWF 5,700) that shows up everywhere. 25+ named category nav links exist (bread, cheeses, craft beers, herbs, oils, salad essentials, etc.) but the store is stocked with exactly one product. Fails the >=5-rows gate. Recorded in `known_blockers.md`. Do not build without re-checking in ~6 months. |
| NISR average retail price dataset | https://www.statistics.gov.rw/ | would be `official_avg` | **NOT REACHED THIS PASS** | Named in the brief as a rule-16 cheap-fetcher option (alongside REG/EUCL tariff and RURA fuel ceilings). Not pursued once the 5/2 bar was cleared by MTN bundles + the two retail spiders — MTN required zero WebSearch budget and zero PDF/table parsing, so it was the cheaper build. Worth a dedicated pass if Rwanda's `official_avg`/`cpi_benchmark` coverage needs to grow: NISR publishes both a CPI series and (per the brief) a separate average-retail-price table, which would need to ship as two manifests (`cpi_benchmark` + `official_avg`, per the "split into two YAMLs" rule) if both exist on the live site. |
| RURA fuel price ceilings / REG-EUCL electricity tariff | (RURA is the regulator for both) | would be `tariff` | **NOT REACHED THIS PASS** | Also named in the brief, also not pursued for the same reason (bar cleared cheaper elsewhere). RURA tariff orders are PDFs per the brief — a `pdf` extraction_pattern fetcher, same shape as the Uganda ERA/Vanuatu URA tariff fetchers already in the repo. Good next-pass candidate for COICOP 04.5 (electricity) and 07.2 (fuel), both currently uncovered for Rwanda. |
| Airtel Rwanda prepaid bundles | (no stable URL found this pass) | would be `tariff` | **NOT REACHED THIS PASS** | Named alongside MTN in the brief as a second telecom tariff option. Not probed — MTN alone cleared the bar and is redundant coverage of the same COICOP 08.1.0 leaf; worth adding only if telecom-tariff cross-checking (two carriers) becomes a priority, not for basket coverage per se. |

## COICOP / channel coverage after this pass

Rwanda: 5 sources / 2 food.
- **01-02 Food and non-alcoholic beverages / alcoholic beverages**: `nhonest_rw` (supermarket, retailer_sku, ~32% food share of a mixed general-merchandise catalog — also carries beer/wine/liquor), `haha_rw` (fresh-market, retailer_sku, 100% food — fresh produce/meat/grain from one physical Kigali market), `fews_net` + `wfp_prices` (official_avg, humanitarian market-survey commodities, pre-existing).
- **08.1 Telephone and internet access services**: `mtn_rw_bundles` (tariff, monthly, prepaid data + voice/data combo bundles).
- **Everything else** (clothing ex-N.Honest's small apparel tail, housing/utilities incl. electricity and water, health, transport ex-fuel, recreation, education, restaurants, insurance, other personal care/misc — COICOP 03-07, 09-13 at the retailer_sku/official_avg/tariff level, plus any `cpi_benchmark` index) remains **uncovered** for Rwanda after this pass. The clearest next-pass leads, in cheapest-first order: (1) RURA fuel-price-ceiling and REG/EUCL electricity-tariff PDF fetchers (07.2 and 04.5 — both named in the wave-12 brief, not reached this pass), (2) NISR CPI and/or average-retail-price fetcher (`cpi_benchmark`/`official_avg`, whole-catalog COICOP-13-division coverage, not reached this pass), (3) Airtel Rwanda prepaid tariff page (redundant COICOP 08.1.0 coverage, lower priority), (4) a fresh discovery pass for Rwanda non-food retail (clothing, household goods, pharmacy/personal-care specialists) — none of the four handed-off candidates were non-food-first, so this axis is still essentially unexplored.
