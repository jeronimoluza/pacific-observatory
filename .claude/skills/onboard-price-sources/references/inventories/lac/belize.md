# Belize

_Inventory written: 2026-09-01_

Wave 13 pass. Cold start — no `lac/belize.md` inventory existed before this file.
Already covered before this pass: `ultraw_bz` (multi-vendor WooCommerce marketplace,
BZD) — 1 source / 0 food. Target: >=5 sources AND >=2 food. Belize is tiny
(~410k people, ISO3 BLZ, currency BZD pegged 2:1 to USD, `src/configs/countries.yaml`
lists only `en`). Both `outputs/sources_pending_will.xlsx` and
`outputs/sources_pending_jero.xlsx` were checked; only `sources_pending_will.xlsx`
had rows, all four already listed in the wave-13 brief (Brodies Belize, MyStore
Belize, DoorStep Belize, Lagniappe Belize Grocery) — no additional candidates found
in either workbook.

## Retail candidates from the brief (entered at Phase 3, discovery skipped)

| Source name | URL | Channel | Status | Notes |
|---|---|---|---|---|
| Brodies | https://brodies.bz/ | pharmacy | **SHIPPED** as `brodies_bz` | Sourcing sheet's URL `brodiesbelize.com` is NXDOMAIN against both 8.8.8.8 and 1.1.1.1 (rule 23 — resolve the domain yourself). Real domain `brodies.bz` resolves fine. Sheet also claimed WordPress/WooCommerce and "Belize's main supermarket chain" — both wrong. Live platform is GoDaddy "Simple Store" (Starfield Website Builder); real catalogue lives on a separate `mysimplestore.com` sub-origin (`/api/v2/products`, JSON, no auth), found via a Playwright network trace of `/shop` — the static HTML has no visible API hints. 61 SKUs total, all Band-Aid/Benadryl/Bengay/Tylenol/Motrin/Imodium/Desitin/reading-glasses — a genuine pharmacy/drugstore at 4 Albert St, Belize City, not a supermarket. `channel: pharmacy` — does not count toward food. Currency explicit `"USD"` per product (no BZ$ prefix), recorded as-is. Full run: 61/61 rows, 0 blanks, 0 zero/negative prices, distinct product_id/url both 61/61, price range $1.33-$1,276.00 (one genuine site data-entry outlier on a Benadryl SKU, re-fetched cold and confirmed unchanged — recorded as displayed, not "corrected"), median $13.22. |
| MyStore Belize | https://mystore.bz/ | — | **DEAD END — app-only, no web catalogue** | Workbook ACCEPT verdict was wrong (rule 20 — six of eight ACCEPTs failed in wave 10 too). Homepage and all sub-pages (`/apps/web/pages/customer.html`, `/store.html`, `/delivery.html`) are pure app-marketing landing pages ("Belize's most trusted grocery and household delivery app") with zero product names, zero prices, zero catalogue links anywhere on the public web surface. Nothing to scrape. |
| DoorStep Belize | https://doorstepbelize.com/ | convenience | **SHIPPED** as `doorstep_bz` | Front page 403s under curl_cffi (chrome120/chrome124/safari17_0) behind a Hostinger edge JS "Checking your browser..." challenge (`server: hcdn`), but Playwright clears it in ~6s — a content-level JS challenge, not a hard WAF (curl_cffi failing alone is not the stop condition; Playwright must also fail). Network trace found the real, UNCHALLENGED backend: `https://api-ecommerce.hostinger.com/store/store_01K7G82RQFP4P73EKX07679YJF/products` — single GET, no auth, no pagination param accepted (`page=` 400s "property page should not exist"), returns the full catalogue in one response. Homepage states "BELMOPAN city only (BEta launch)" — small single-city delivery startup, not a chain, hence `channel: convenience` rather than `supermarket` (workbook's "Belmopan only, catalogue thin" note was directionally right on locality, wrong that it's too thin to use — 204 SKUs cleared the bar easily). Roughly half food (Flavora/Kraft/DAK/Takis/Coca-Cola/bread/rice/ice), half household/pharmacy (diapers, pads, cleaners, condoms). Explicit nested `currency_code: "bzd"`, values in MINOR UNITS (cents) — confirmed via `min_amount: 200` = BZ$2.00. Full run (single unpaginated response = the whole catalogue): 204/204 rows, 0 blanks, 0 zero/negative prices, distinct product_id/url both 204/204, all BZD, price range $1.25-$120.00, median $6.85. |
| Lagniappe Belize Grocery | http://www.belizegrocery.com/ | specialty-food | **SHIPPED** as `lagniappe_bz` | Sheet's plain-HTTP URL is live but redirects via a `<base href>` tag (not an HTTP 301) to the canonical `https://www.lagniappebelize.com/` — same IP, same ClickCartPro/Kryptronic backend, HTTPS-capable. NOT stale despite the HTTP-only sheet URL: homepage carries a dynamic operational notice about 3-day lead times and ZELLE payment, i.e. currently trading. Villa/tourist provisioning grocery on Ambergris Caye (San Pedro) with a narrow imported/gourmet range (wine by country of origin, spirits, cheese, deli, bakery) — `channel: specialty-food`. Sells INTO Belize so it clears the locality bar despite serving mostly tourists/villa renters. Tier 1A server-rendered HTML, no JSON API; ~90 leaf category refs hardcoded from a one-time nav scrape. **Gotcha that cost most of the build time**: the site alternates, per-response, between a query-string product link (`?ns=prodshow&ref=<ref>`) and a pretty-path link (`/Item/<ref>`) for the SAME product — confirmed on two fetches of `/Category/snack` seconds apart. A regex matching only the query-string form silently returned 0 items for every category except the first two hit in a crawl, while the surrounding name/price markup still parsed fine (a false-negative that looked like a real result, not an error) — fixed by matching either href form. Full unbounded run: 1075 unique rows (182 cross-category duplicate URLs correctly dropped by DuplicationPipeline), 0 blanks, 0 zero/negative prices, all USD, price range $0.35-$299.00, median $10.95, strongly food/beverage-led category mix (condiments 50, snack 49, dairy 46, breakfast 44, canned veg 40, deli 39) plus a large wine/spirits tail. |

## Non-retail candidates (from the brief's DISCOVER list)

| Source name | URL | Channel | Status | Notes |
|---|---|---|---|---|
| SIB Monthly CPI | https://sib.org.bz/statistics/economic-statistics/consumer-price-index/ | null (cpi_benchmark) | **SHIPPED** as `sib_cpi` | The CPI page itself only embeds a technical-note PDF and a 2-column "average prices of selected foods and fuel" snapshot table in static HTML — the actual downloadable XLSX time series is loaded by a client-side JS "resources repository" widget with empty `<div data-sibr>` placeholders, discovered via a Playwright network trace: `wp-json/sibr/v1/resources?topic=consumer-price-index-statistics` returns a stable `/dl/monthly-consumer-price-index/` redirect to the current XLSX. "EXTENDED SERIES" sheet is a genuine COICOP-2018 13-division monthly index, October 2020=100, Nov 1990 through Jul 2026 — SIB's own division labels map 1:1 onto COICOP 2018 in order, no translation table needed. Headline "All Items" column dropped (no sanctioned `coicop_code` sentinel for headline CPI yet). Full run: 3,471 rows, 0 duplicate `observation_hash`. |
| Belize Ministry of Finance / Supplies Control Unit fuel prices | (not found) | — | **NOT FOUND THIS PASS** | No public web page locating a Supplies Control Unit maximum-retail-fuel-price schedule was found within this pass's probe budget (a mof.gov.bz domain resolves, but no fuel-price page was located on it without spending WebSearch budget the pass didn't need once 6 sources had already landed). Worth a follow-up pass — Belize almost certainly has SOME administered fuel-price mechanism given the region's pattern, this is a "not found", not a "does not exist". |
| BEL (Belize Electricity Limited) residential tariff | https://www.bel.com.bz/Rate_Schedule.aspx | null (tariff) | **SHIPPED** as `bel_tariff` | Old absolute-positioned ASPX layout (`bel.com.bz` — homepage itself is a legacy embedded-iframe/absolute-position design), but the rate table is plain HTML parsed cleanly by `pandas.read_html`, no Playwright needed. Effective period stated in plain text on the page. **Gotcha**: Social/Residential/Commercial 1 rates all share ONE physical `<table>` element — a naive match on "RESIDENTIAL RATES" text anywhere in that table pulled in Social's and Commercial 1's identically-labeled "1"/"2"/"3"/"Minimum Charge" rows too (5 of 11 rows were exact-duplicate hashes on the first attempt); fixed by slicing rows strictly between each section's header cell and its own "Minimum Charge" row. Ships both household-facing classes (Residential + Social/lifeline); Commercial/Industrial/Street Lights excluded as non-household. Full run: 6 rows, 0 duplicate `observation_hash`. Residential block 1 (0-50 kWh) $0.34/kWh, block 2 $0.42, block 3 $0.46, minimum $10.00; Social block 1 (0-60 kWh) $0.22/kWh, minimum $5.00 — all BZD, effective 2026-01-01 to 2026-06-30. |
| Digi Belize / Smart prepaid mobile plans | (not probed) | — | **NOT BUILT this pass** | Listed in the brief as a cheap static-HTML build. Not probed — the 5-source/2-food bar was already cleared with 6 sources / 2 food from the five rows above, and probe budget was spent instead on fixing the Lagniappe href-format bug and the BEL combined-table dedup bug. Left for a future pass wanting more `tariff` depth. |
| PUC Belize water tariffs (BWS) | (not probed) | — | **NOT BUILT this pass** | Same reasoning as Digi/Smart above — `bws.bz` resolves live (confirmed via DNS) but was not probed this pass. Left for a future pass. |

## Outcome after this pass

Belize ends at **6 sources / 2 food**: `ultraw_bz` (marketplace, pre-existing),
`brodies_bz` (pharmacy), `doorstep_bz` (convenience, FOOD), `lagniappe_bz`
(specialty-food, FOOD), `sib_cpi` (cpi_benchmark, null channel), `bel_tariff`
(tariff, null channel). Both bars cleared with one source of margin above the
5-source floor. Two non-food, cheap DISCOVER candidates from the brief (Digi/Smart
mobile plans, PUC water tariffs) and one candidate that was not found this pass
(Ministry of Finance fuel prices) remain open for a future pass wanting more
`tariff`/`aggregate_proxy` depth — none were needed to clear this wave's bar.
MyStore Belize (workbook ACCEPT) was confirmed dead (app-only, no web catalogue) —
the workbook's platform/verdict columns were wrong on 2 of 4 retail rows this pass
(Brodies' platform guess, MyStore's ACCEPT verdict), consistent with rules 20/23.
