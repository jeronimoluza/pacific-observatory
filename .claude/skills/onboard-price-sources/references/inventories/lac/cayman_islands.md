# Cayman Islands

_Inventory written: 2026-09-01_

Wave 9 pass. Cold start — no `lac/` inventory existed for Cayman Islands before this
file, and `outputs/sources_pending_jero.xlsx` carries no rows for this country
(confirmed by reading the workbook directly before starting). Already covered before
this pass: `costuless_ky` (`channel: wholesale` — does not count toward food) — 1
source / 0 food. Target: >=5 sources AND >=2 food. Entered at Phase 3 (probe) for the
brief's named leads (Foster's, Kirk Market, Hurley's, ESO, CUC, Water Authority,
Flow/Digicel/C3); no fresh discovery search beyond those named leads plus one
follow-on find (OfReg) reached from CUC's own site.

| Source name | URL | Channel | Status | Notes |
|---|---|---|---|---|
| Foster's Food Fair | https://shop.fosters.ky/ | supermarket | **SHIPPED** as `fosters_ky` | fosters.ky (brochure site) 403s under `curl_cffi impersonate=chrome124/chrome120` but clears with `chrome99`/`safari17_0`/`edge99` — a TLS-fingerprint-sensitive WAF, not a hard block (see known_blockers.md note added this wave). Live catalogue is NCR Freshop (`app_key=fosters`, same platform as `costuless_ky`), store_id=3748 (Camana Bay flagship, 26,894 SKUs). A second selectable store (`store_id=6642`, "Priced Right" discount banner, 2,768 SKUs, different prices for overlapping items) exists under the same app_key — NOT onboarded separately, to avoid double-counting the same brand's shelf. Currency: KYD — the API gives no `currency_code` field (plain "$" string, same gap as `costuless_ky`), but independent evidence (Cayman Resident consumer coverage, via web search) confirms Cayman grocery prices are quoted in CI$, not USD; the US-warehouse-chain assumption used for `costuless_ky` does not apply to a domestic Cayman chain. **Rate-limit note:** the shared `api.freshop.ncrcloud.com` host trips a `400 {error_code:429}` under concurrent multi-agent/fleet load partway through a full-catalog walk, self-healing given enough time (see `known_blockers.md`) — **measured** plateau of 9,200 distinct SKUs (34% of catalog) in one continuous run; 59.1% food-department share, 100% KYD, 0 zero-price/blank-name rows, 3/3 cold re-fetch spot checks matched. |
| Kirk Market | https://kirkmarket.ky/ | specialty-food | **SHIPPED** as `kirkmarket_ky` | kirkmarket.ky is a WordPress brochure site; its only live transactable catalogue is a small WooCommerce "special orders"/catering shop — 40 SKUs total (pre-made platters, cakes, pastries), verified via `X-WP-Total: 40` on the Store API. No everyday grocery-aisle SKU browsing exists on this domain (unlike Foster's/Hurley's, which run full Freshop storefronts) — labelled `specialty-food`, not `supermarket`, to reflect that honestly. Currency: KYD, read directly from the Store API's own `prices.currency_code` field (no inference needed). **Measured:** all 40/40 SKUs collected (complete catalog), 100% food-category (platters/cakes/pastries), 100% KYD, 0 zero-price/blank-name, 1/1 cold re-fetch spot check matched. |
| Hurley's Marketplace | https://shop.hurleys.ky/ | supermarket | **SHIPPED** as `hurleys_ky` | Same NCR Freshop platform as Foster's (`app_key=hurleys`). Two selectable stores: `store_id=1874` ("Hurley's" flagship, 17,074 SKUs — used here) and `store_id=7634` ("Hurley's DASH" delivery-service pricing, 17,048 SKUs, overlapping catalogue) — DASH not onboarded separately, same reasoning as Foster's Priced Right exclusion. Currency: KYD, same basis as `fosters_ky` (no `currency_code` field on the API; independent evidence that Cayman groceries price in CI$). **Measured:** the same shared rate limit that affected `fosters_ky` hit this source harder — repeated attempts on the same day (4 separate runs) all plateaued around 900 distinct SKUs (5.3% of catalog), a smaller sample than `fosters_ky` got, confirming the limit is shared/contention-driven rather than a fixed per-source cap. 41.4% food-department share among sampled rows; a further 46.3% have no category at all because those product URLs skip the department path segment on Hurley's own site (genuine site characteristic, not a spider parsing bug). 100% KYD, 0 zero-price/blank-name, 2/2 cold re-fetch spot checks matched. |
| ESO CPI by Divisions | https://www.eso.ky/indicators-page.html | null (cpi_benchmark) | **SHIPPED** as `eso_cpi` | Cayman's ESO (Economics and Statistics Office) publishes a quarterly "CPI by Divisons <Nq><YYYY>.xlsx" on a static indicators page, overwritten in place each quarter. **Gotcha found and fixed:** the single-sheet workbook stacks TWO title blocks — an original "(JUNE 2008 = 100)" series (2009-2017) and a rebased "(SEPTEMBER 2016 = 100)" series (2009-2025) — both covering overlapping years at different index levels. Naively parsing the whole sheet double-counts every overlapping quarter with two conflicting values sharing one identity. Fixed by anchoring on the LAST title block only (same "anchor on the last occurrence" pattern used for regulator PDFs with a superseded earlier table). Also found: the rebased block renamed one column ("Household Equipment" -> "Household Furnishings & Equipment") — needed an alias in the COICOP map or division 05 silently drops. 12 major groups + ALL ITEMS (headline dropped, no sanctioned coicop_code sentinel), no division 13, quarterly cadence, 2009 Q1-2025 Q4 (68 quarters) = 816 rows verified clean (no dupes) after the fix. No anti-bot — plain `requests` works. |
| OfReg weekly retail fuel prices | https://www.ofreg.ky/fuel/retail-fuel-prices | null (official_avg) | **SHIPPED** as `ofreg_fuel` | Found via CUC's own site nav (CUC itself had no discoverable rate/tariff page — see below) — the utility/fuel regulator (Utility Regulation and Competition Office) publishes a weekly "Retail Fuel Prices Report" PDF, surveyed from gas stations and marinas across all three islands. Reads the report's own pre-aggregated "Retail Network Average Weekly Price Analysis" table (Regular/Premium Gasoline, Diesel) rather than re-deriving an average from the messy per-brand per-station tables (which have inconsistent column layouts per brand block). Listing page carries a rolling ~12-report window (~3 months); fetcher backfills everything currently listed. **Currency: KYD, stated explicitly and unambiguously in the report's own text** ("...provided in Cayman Islands dollars per imperial gallon") — the one source onboarded this wave with zero currency ambiguity to resolve, unlike every retail-catalogue source above. |
| CUC (Caribbean Utilities Company) | https://www.cuc-cayman.com/ | — | **NOT PURSUED — no rate schedule discoverable in site nav** | Homepage nav has no "rates"/"tariff" link; checked `/electrical-service` and `/regulatory-submissions` pages directly — no rate table, no PDF tariff schedule, no fuel-factor figure found on either. Not exhaustively probed (did not open every "reports"/"download-file" link) — deprioritized once the 5-source/2-food bar was cleared via OfReg instead. Worth a deeper pass (site-map crawl, or the `reports` page's document list) if a future wave still needs a Cayman electricity tariff. |
| Water Authority Cayman | https://www.waterauthority.ky/ | — | **INCONCLUSIVE — not pursued** | Site nav has promising-looking `/fees-charges` and `/customer-connect-rates` pages, but `/fees-charges` renders almost entirely client-side (curl-fetched HTML is page-shell JS/jQuery boilerplate with no visible rate table in the static markup) — would need a Playwright render to confirm whether a real per-unit water tariff exists. Not pursued further once the bar was cleared. Flagged as the best next non-food lead for Cayman. |
| Flow Cayman | flow.com.ky (brief's guessed domain) | — | **DEAD — domain does not resolve** | `flow.com.ky` returns NXDOMAIN against both `8.8.8.8` and `1.1.1.1` (not a sandbox DNS artifact — confirmed against two independent public resolvers per the "sandbox DNS can lie" rule). The real regional Flow storefront is likely `discoverflow.co` (resolves fine, Cloudflare-fronted) — not probed this wave; a Cayman-specific price catalogue on that domain is unconfirmed. |
| Digicel Cayman | https://www.digicelcayman.com/ | — | **UNRESOLVED — TLS/host-config issue, not diagnosed** | Domain resolves fine via public DNS. `curl_cffi impersonate=chrome99` returns HTTP 421 (Misdirected Request) even with `verify=False` — looks like an SNI/host-header mismatch or a Cloudflare/host misconfiguration rather than a WAF block, but not diagnosed further this wave. Worth a `chrome124`/`safari17_0` re-probe and a check of whether a different Digicel Caribbean domain hosts the Cayman plan-pricing page instead. |
| C3 Pure Fibre | c3pf.com / c3purefibre.ky / c3pure.ky (guessed) | — | **DEAD (guessed domains) / UNCONFIRMED (`c3.ky`)** | All three guessed domain forms are NXDOMAIN against both `8.8.8.8` and `1.1.1.1`. `c3.ky` DOES resolve, but was not verified this wave to actually be C3 Pure Fibre's site (vs. an unrelated registrant) — not probed further once the bar was cleared. |

## COICOP / channel gap after this pass

Cayman Islands ends at **6 sources / 3 food** (`fosters_ky` supermarket, `hurleys_ky`
supermarket, `kirkmarket_ky` specialty-food), clearing both the 5-source and 2-food
bars with one food source and one whole non-retail source to spare. `costuless_ky`
(wholesale) and `eso_cpi` (cpi_benchmark) / `ofreg_fuel` (official_avg, fuel 07.2.2)
round out the non-food half.

Remaining gaps for a future pass: electricity tariff (CUC's own site has no
discoverable rate page — would need a deeper crawl or a call to OfReg's own site for
an electricity-specific filing, parallel to how the fuel report was found there), water
tariff (Water Authority's `/fees-charges` page needs a Playwright render, not just a
curl fetch, to confirm whether it holds a real per-unit table), and telecom tariffs
(Flow's real domain is unconfirmed, Digicel Cayman's domain has an unresolved
421/host-config quirk, and C3 Pure Fibre's real domain was never identified). None of
these block this wave's target, but all three are plausible cheap non-food wins for
whichever wave revisits Cayman next.
