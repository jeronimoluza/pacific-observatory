---
phase: 00.5-cascade-cleanup-behavior-preserving
plan: 01
subsystem: prices-enrich
tags: [coicop, sub-labels, knn, taxonomy, dead-code, inventory, parity, coverage]

# Dependency graph
requires:
  - phase: 00-eval-foundation-independent-gold-set
    provides: 313-row working gold + prices eval harness (the parity reference)
provides:
  - PARITY-ANCHOR.md — D-09 before-baseline (commit SHA 48b1a750 + per-dimension eval table)
  - INVENTORY.md — SC1 read-only blast-radius map (dead paths, knob map, taxonomy data-flow, file-split candidates)
  - APPROVE-ONCE list (bakeoff.py, replay.py, D-07 intra-branch candidates) for Plan 06
  - Decisive D-01 finding — _sub_labels.parquet is a LOSSY projection (drops allowed_bases), cannot be SoT as-is
affects: [00.5-02, 00.5-03, 00.5-04, 00.5-05, 00.5-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "coverage-from-eval as the static dead-branch signal (vulture unavailable)"
    - "force-add gitignored .planning/ artifacts for plan commits (commit_docs override)"

key-files:
  created:
    - .planning/phases/00.5-cascade-cleanup-behavior-preserving/PARITY-ANCHOR.md
    - .planning/phases/00.5-cascade-cleanup-behavior-preserving/INVENTORY.md
  modified: []

key-decisions:
  - "Parity anchor SHA is 48b1a750 (one checkpoint commit ahead of BASELINE.md's 99c18a88); eval numbers reproduce BASELINE.md exactly — checkpoint did not touch any decision path"
  - "_sub_labels.parquet is a LOSSY 5-column projection (drops allowed_bases, explodes keywords_by_lang) — it CANNOT become the D-01 single source of truth as-is; Plan 02 must widen the schema or use a regenerate-from-xlsx loader"
  - "c{NN}_subs.py header 'Source: _sub_labels.parquet' is STALE/misleading; the real current direction is xlsx → c{NN}.py + c{NN}_subs.py → _sub_labels.parquet (py is today's truth)"
  - "Coverage-from-eval flags unexercised, not dead: tier-c path (OFF in baseline), build path (cli.py), and defensive guards dominate the uncovered lines — all route to Plan 06 approve-once, none auto-delete"

patterns-established:
  - "Pattern 1: every dead-code candidate carries grep caller-evidence + a gold-exercised vs gold-unexercised tag"
  - "Pattern 2: read-only discovery proven by git diff --quiet -- src/prices"

requirements-completed: [SC1, SC2, D-01, D-04, D-06, D-09]

# Metrics
duration: ~22min
completed: 2026-06-18
status: complete
---

# Phase 00.5 Plan 01: Discovery Inventory & Parity Anchor Summary

**Read-only blast-radius map of `src/prices/enrich/` (dead paths, 6-knob map, dual-family taxonomy data-flow, 18 over-500 files) plus the D-09 parity anchor (SHA 48b1a750, coicop 23.32% on the 313-row gold), with the decisive finding that `_sub_labels.parquet` is a lossy projection that cannot be the D-01 source of truth as-is.**

## Performance

- **Duration:** ~22 min
- **Started:** 2026-06-18T18:04:55Z
- **Completed:** 2026-06-18T18:27Z
- **Tasks:** 2
- **Files modified:** 2 created (both under `.planning/`); zero `src/` edits

## Accomplishments
- Captured the D-09 before-baseline: commit `48b1a750` + the per-dimension `prices eval --no-write` table (coicop 73/313=23.32%, sub_label 9/313=2.88%, basis 287/313=91.69%, std_unit 287/313=91.69%, unit_value 281/313=89.78%) — byte-identical to Phase 0 BASELINE.md.
- Built the SC1 INVENTORY with all four sections, each grep/coverage-backed.
- Confirmed `bakeoff.py` (522) + `replay.py` (334) have zero importers and 0% coverage → APPROVE-ONCE.
- Produced the D-07 intra-branch candidate list from a STATIC tool (coverage of the 313-gold eval, since `vulture` is not installed) with file:line ranges + caller-evidence spot-checks.
- Mapped all 6 knobs as single-defined in `config.py` (flagged the `getattr(config,"MIN_SAME_CHANNEL_KNN",3)` stale-default risk for D-04).
- Resolved the taxonomy data-flow direction and surfaced the lossy-parquet finding that gates D-01.

## Task Commits

1. **Task 1: Capture prices eval parity anchor (PARITY-ANCHOR.md)** - `b1f41b04` (docs)
2. **Task 2: Map dead paths, knobs, taxonomy data-flow, file-split candidates (INVENTORY.md)** - `174cd0b6` (docs)

**Plan metadata:** (this SUMMARY + STATE/ROADMAP) committed separately.

## Files Created/Modified
- `.planning/phases/00.5-cascade-cleanup-behavior-preserving/PARITY-ANCHOR.md` - D-09 before-baseline: SHA + per-dimension eval table + delta vs BASELINE.md.
- `.planning/phases/00.5-cascade-cleanup-behavior-preserving/INVENTORY.md` - SC1 discovery: dead paths (incl. coverage-derived D-07 list), knob map, dual-family taxonomy data-flow + store coverage, file-split candidates with dispositions, APPROVE-ONCE list.

## Decisions Made
- **Parity SHA = `48b1a750`, not `99c18a88`:** HEAD is one checkpoint commit ahead of BASELINE.md; eval numbers reproduce exactly, so the checkpoint touched no decision path. The anchor records `48b1a750` as the true tree state cleanup begins from, with a documented delta table.
- **`_sub_labels.parquet` is LOSSY (decisive for D-01):** `regenerate_sub_labels_parquet.py` emits only 5 columns and drops `allowed_bases` + the `keywords_by_lang` tuple grouping, so the parquet cannot reconstruct the full SubLabel record. Recommended store shape: keep two artifacts (hierarchical class-tree store + a schema-widened/flat sub-label store) OR a regenerate-from-xlsx loader — Plan 02 must NOT adopt the current 5-column parquet as truth.
- **Coverage ≠ dead:** the uncovered-line table is dominated by the tier-c LLM path (OFF in the deterministic baseline), the index-build path (`cli.py`, not eval), and defensive guards — all routed to Plan 06 approve-once, none auto-deleted.

## Deviations from Plan

None - plan executed exactly as written. Two plan-anticipated adaptations were applied:
- `vulture` is not installed (the plan pre-authorized the coverage fallback for D-07) — used `poetry run coverage run --source=src/prices/enrich run.py prices eval --no-write` + `coverage report --show-missing`.
- The plan's `read_first` hint said sub_labels are "injected from `_sub_labels.parquet` sidecar" at registry load; the actual code (`_registry.py`) imports `SUB_LABELS_BY_LEAF` from the `c{NN}_subs.py` Python modules, while `_sub_labels.parquet` is read separately by `index.py`/`taxonomy_index.py`. The inventory records the corrected dual-consumer data-flow.

## Issues Encountered
- First coverage attempt via `python -m prices.enrich.cli eval` produced no scorecard (that module has no `eval` subcommand; the eval lives at `prices.enrich.eval.cli:eval_command`). Re-ran coverage wrapping `run.py prices eval --no-write` directly, which instrumented the real eval path (same 73/313 numbers). The `.coverage` data file was removed after the report; `git diff --quiet -- src/prices` stayed clean.

## User Setup Required
None - no external service configuration required. (`prices eval` was run only with `--no-write`; persisting the report under `outputs/` remains a user action per data-safety.)

## Next Phase Readiness
- SC1 inventory + D-09 parity anchor are in place; plans 02–06 can begin against a known blast radius and a fixed before-baseline.
- **Hand-off flag for Plan 02:** treat `_sub_labels.parquet` as lossy — do NOT make the current 5-column parquet the source of truth; widen the schema or use the xlsx regenerate loader to preserve `allowed_bases`.
- **Hand-off flag for Plan 03:** the `getattr(config,"MIN_SAME_CHANNEL_KNN",3)` site at `index.py:514` must be kept in sync when the knob moves to YAML.
- **Hand-off flag for Plan 06:** APPROVE-ONCE inputs = `bakeoff.py`, `replay.py`, and the §1b intra-branch candidates (each needs a `dead` vs `live-but-unexercised` verdict).

## Self-Check: PASSED

- FOUND: PARITY-ANCHOR.md, INVENTORY.md, 00.5-01-SUMMARY.md
- FOUND commits: b1f41b04 (parity anchor), 174cd0b6 (inventory), 76e6842f (summary)
- `git diff --quiet -- src/prices` clean throughout (zero source edits)

---
*Phase: 00.5-cascade-cleanup-behavior-preserving*
*Completed: 2026-06-18*
